development:
  adapter: postgresql
  database: combat_dev
  username: postgres
  password: password
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &TEST
  adapter: postgresql
  database: combat_test
  username: postgres
  password: password
  host: localhost

  production:
    adapter: postgresql
    database: combat_prod
    username:
    password:
    host: localhost
